@AGENTS.md

## Claude Code

`.claude/` holds Claude Code's runtime config (settings, hooks, commands,
output styles, rules), plus symlinks into `.agents/skills` and `.agents/agents`
for the portable skill and subagent definitions described in `AGENTS.md`.
