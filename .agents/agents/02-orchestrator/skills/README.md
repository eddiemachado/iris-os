# orchestrator — private reference docs

Empty for now. If orchestrator-specific procedural knowledge accumulates (e.g. a repo-family-specific runbook), it goes here as plain markdown, read via the `Read` tool per `agent.md`'s instructions — **never** as a `SKILL.md`, and never placed under `~/.claude/skills/` or `<repo>/.claude/skills/`. Those paths get scanned into every session's context regardless of whether that session ever uses the content; this folder doesn't.
