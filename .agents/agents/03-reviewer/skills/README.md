# reviewer — private reference docs

Empty for now. Same rule as orchestrator's: agent-private procedural knowledge goes here as plain markdown, read via the `Read` tool per `agent.md`'s instructions — never as a `SKILL.md`, never under `~/.claude/skills/` or `<repo>/.claude/skills/`, since those get scanned into every session's context regardless of use.
