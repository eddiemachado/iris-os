---
summary: >
  TODO - spacing scale and usage rules for padding/margin/gap. (file is
  empty, pending content)
triggers: [spacing, padding, margin, gap, scale]
related: [layout.grid]
---

# Spacing

- TODO: add rules


## Padding

Use [TODO: token location] for applying padding to an element. **Never** use hard coded values (e.g. `8px`) or primitive tokens (e.g. `spacing-8`) for padding. **Never** use `margin` or `gap` tokens for applying padding.

- TODO: add rules


## Margins

Use [TODO: token location] for applying margin to an element. **Never** use hard coded values (e.g. `8px`) or primitive tokens (e.g. `spacing-8`) for margins. **Never** use `padding` or `gap` tokens for applying margins.

- TODO: add rules


## Gaps

Use [TODO: token location] for applying gaps to an element. **Never** use hard coded values (e.g. `8px`) or primitive tokens (e.g. `spacing-8`) for gaps. **Never** use `padding` or `margin` tokens for applying margins.

- TODO: add rules